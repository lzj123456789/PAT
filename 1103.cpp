#include <bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
	int n,k,p;
	scanf("%d %d %d",&n,&k,&p);
	std::vector<int> v(k);
	for(int i=1;i*i<=n;i++){
		int m = 0,j=i;
		int tmp = n;
		while(tmp){
			int ni = pow((double)j,i);
			if()
		}
	}
	return 0;
}